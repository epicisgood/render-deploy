name: Publish image to Docker Hub

on:
  [workflow_dispatch]

jobs:
  publish_image:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: build
        run: |
          docker build . -t fort30/fort30:latest
      - name: publish
        run: |
          docker login -u fort30 -p ${{ secrets.DOCKERHUB_TOKEN}}
          docker push fort30/fort30:latest
